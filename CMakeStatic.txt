SET(CMAKE_SYSTEM_NAME ${EE_STATIC_SYSTEM})

IF(EE_CROSS_COMPILE)
    SET(CMAKE_C_COMPILER ${EE_STATIC_C_COMPILER})
    # SET(CMAKE_CXX_COMPILER ${EE_STATIC_CXX_COMPILER})
    # SET(CMAKE_AR ${EE_STATIC_AR})
    # SET(CMAKE_RANLIB ${EE_STATIC_RANLIB})
    # SET(CMAKE_LINKER ${EE_STATIC_LINKER})
    IF(CMAKE_SYSTEM_NAME STREQUAL "Windows")
        SET(CMAKE_RC_COMPILER ${EE_STATIC_RC_COMPILER})
    ENDIF(CMAKE_SYSTEM_NAME STREQUAL "Windows")
ENDIF(EE_CROSS_COMPILE)

SET(CMAKE_FIND_ROOT_PATH
    ${EE_STATIC_BIN_PREFIX}
    ${EE_STATIC_ROOT_PREFIX}
    ${EE_STATIC_DEPS}
)

SET(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM ONLY)
SET(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
SET(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
SET(EE_STATIC 1)

## vim:sw=4 ts=4 syntax=cmake et:

